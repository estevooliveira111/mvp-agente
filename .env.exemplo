# ==========================================
# CONFIGURAÇÕES DA IA E MODELOS (LLM)
# ==========================================
# Chave da OpenAI para uso do GPT-4o e GPT-3.5
OPENAI_API_KEY=sk-suachaveaqui123456789

# Chave do Google Gemini (Modelos Gemini 1.5 Pro / Flash)
GEMINI_API_KEY=AIzaSySuaChaveGeminiAqui123

# Chave da Anthropic para uso do Claude 3 (Opcional, caso troque o provedor no main.py)
ANTHROPIC_API_KEY=sk-ant-suachaveaqui123

# ==========================================
# CONFIGURAÇÕES DOS CANAIS (MENSAGERIA)
# ==========================================
# Token do Bot criado no @BotFather no Telegram
TELEGRAM_BOT_TOKEN=0000000000:AAH_SeuTokenTelegramAqui_xyz

# URL pública da API (ex: ngrok). Usada para registrar o webhook do Telegram.
WEBHOOK_URL=

# Segredo que o Telegram devolve em cada update. Fora de 'development', sem ele o
# webhook recusa tudo. Gere com: openssl rand -hex 32
TELEGRAM_WEBHOOK_SECRET=SeuSegredoDoWebhookAqui

# ==========================================
# BANCO DE DADOS RELACIONAL (POSTGRESQL)
# ==========================================
# Configurações do Docker-Compose
POSTGRES_USER=admin
POSTGRES_PASSWORD=adminpassword
POSTGRES_DB=mvp_agente
POSTGRES_HOST=localhost
POSTGRES_PORT=5432

# ==========================================
# MEMÓRIA VETORIAL (CHROMADB) E CACHE
# ==========================================
CHROMADB_HOST=localhost
CHROMADB_PORT=8000

MEMCACHED_HOST=localhost
MEMCACHED_PORT=11211

# ==========================================
# SEGURANÇA E SISTEMA
# ==========================================
# Chave Fernet de 32 bytes em Base64 para a ferramenta de criptografia. 
# Você pode gerar uma rodando: python3 -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
ENCRYPTION_KEY=SuaChaveFernetBase64Aqui=

# Única pasta que a ferramenta file_read pode ler (relativa à raiz do projeto).
FILE_READ_BASE_DIR=data/files

# Nível de Log (DEBUG, INFO, WARNING, ERROR)
LOG_LEVEL=INFO
DISCORD_BOT_TOKEN=

# ==========================================
# AUTENTICAÇÃO (JWT) DAS ROTAS REST
# ==========================================
# Chave secreta usada para assinar os tokens JWT das rotas /api/v1/chat.
# Gere uma com: openssl rand -hex 32
JWT_SECRET_KEY=SuaChaveSecretaJWTAqui

# Validade do token de acesso, em minutos.
ACCESS_TOKEN_EXPIRE_MINUTES=60
